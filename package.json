

{
  "name": "pickmyfit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node ./server/index.js",
    "build": "npm install &&  npm run migrate && npm run seed && cd client && npm run build ",
    "dev": "nodemon ./server/index.js",
    "make:migration": "knex migrate:make",
    "migrate": "knex migrate:latest",
    "make:seed": "knex seed:make",
    "seed": "knex seed:run"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "knex": "^2.3.0",
    "pg": "^8.8.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
